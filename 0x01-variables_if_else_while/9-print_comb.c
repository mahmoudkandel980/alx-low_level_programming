
#include <stdio.h>

/**
* main - Main Entry
* Return: 0 (success)
*/
int main(void)
{
	int number;

	for (number = 48; number < 58; number++)
		putchar(n);
		if (n != 57)
			putchar(',');
			putchar(' ');
	putchar('\n');
	return (0);
}
