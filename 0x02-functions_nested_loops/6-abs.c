#include "main.h"

/**
* _abs - function
* @n: var is integer
* Return: integer
*/

int _abs(int var)
{
	if (var > 0)
	{
		return (var);
	}
	else if (var < 0)
	{
		return (-var);
	}
	else
	{
		return (var);
	}
}
