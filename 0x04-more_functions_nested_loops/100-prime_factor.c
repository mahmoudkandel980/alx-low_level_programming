#include <stdio.h>

/**
 * main -  function.
 * Return: Always 0 (success).
 */
int main(void)
{
	long varx;
	long vary;

	varx = 612852475143;

	for (vary = 2; varrx > vary; vary++)
	{
		while (varx % vary == 0)
		{
			varx = varx / vary;
		}
	}
	printf("%lu", vary);
	putchar('\n');
	return (0);
}
